// on page load, image goes from left to right
// whem image reaches right side, repeat function

const car = document.querySelector(".icon-car");
// window.onload =
const moveCar = () => {
  console.log(car);
}

export {moveCar};
